name: Deploy Production

on:
  workflow_dispatch:
  push:
    branches: [main]

concurrency:
  group: deploy-prod-backend
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install, test and build API
        run: |
          npm ci
          npx prisma generate
          npm run test -- --passWithNoTests
          npm run build

      - name: Upload API artifact
        uses: actions/upload-artifact@v4
        with:
          name: domo-api-prod
          path: |
            dist
            package.json
            package-lock.json
            prisma

  # Conecta tu hosting (Railway / Render / Fly / VPS).
  # Ejemplo conceptual Railway:
  #
  # deploy:
  #   needs: build
  #   runs-on: ubuntu-latest
  #   environment: production
  #   steps:
  #     - uses: actions/download-artifact@v4
  #       with:
  #         name: domo-api-prod
  #     - name: Deploy API + migrate
  #       run: |
  #         # Opción A: imagen Docker (entrypoint ya hace migrate)
  #         # docker build -t domo-api . && docker push …
  #         # Opción B: SSH / CLI del proveedor
  #         echo "Deploy API + worker. Worker: node dist/worker.js"
  #     - name: Reminders
  #       run: |
  #         echo "Set NODE_ENV=production JWT_* COOKIE_SECURE TRUST_PROXY REDIS_URL"
  #         echo "Set EMAIL_PROVIDER=resend EMAIL_VERIFICATION_ENABLED=true"
  #         echo "Set METRICS_TOKEN and keep SWAGGER_ENABLED=false"
  #         echo "Run a separate worker service with the same image: node dist/worker.js"
